global:
  resolve_timeout: 5m
route:
  receiver: "default"
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h
receivers:
  - name: "default"
    webhook_configs:
      - url: "http://127.0.0.1:5001/"
        send_resolved: true
